"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const RandomBalancer_1 = require("./RandomBalancer");
test('variation of 10K requests', () => {
    const balancer = new RandomBalancer_1.RandomBalancer(10);
    const bins = new Array(10);
    for (let i = 0; i < bins.length; i++) {
        bins[i] = 0;
    }
    for (let i = 0; i < 1e4; i++) {
        bins[balancer.pick()]++;
    }
    for (const bin of bins) {
        expect(bin / 1e4).toBeCloseTo(0.1, 1);
    }
});
//# sourceMappingURL=RandomBalancer.spec.js.map